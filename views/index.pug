doctype html
html
  head
    link(rel="stylesheet" href="/style.css")

    title= title
  body
  h1= title
  a.btn.btn-primary(href='/events/new') Create New Event
  ul
    each event in events
      li
        h2= event.name
        p= event.description
        p= `Date: ${event.date}`
        form(method='POST' action=`/events/${event._id}?_method=DELETE`)
          button.btn.btn-danger(type='submit') Delete
        form(method='GET' action=`/events/${event._id}/edit`)
          button.btn.btn-warning(type='submit') Edit
  p= "This web application was created to fulfill Web Technology module’s requirements and does not represent an actual company or service"